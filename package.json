{
  "name": "gcr.node.ts",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "cross-env ENV_TYPE=\"dev\" ts-node src/main.ts",
    "dev:watch": "cross-env ENV_TYPE=\"dev\" ts-node-dev --respawn --pretty --transpile-only src/main.ts",
    "start": "tsc && node ./dist/main.js"
  },
  "author": "gentom",
  "license": "MIT",
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^16.3.1",
    "ts-node": "^10.1.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "express": "^4.17.1"
  },
  "engines": {
    "node": ">= 12.0.0"
  }
}
